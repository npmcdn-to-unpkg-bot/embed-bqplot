[
    "from bqplot import pyplot as plt",
    "import numpy as np",
    "",
    "plt.figure()",
    "n = 100",
    "x, y = np.linspace(0.0, 10.0, n), np.cumsum(np. random.randn(n))",
    "plt.plot(x, y, axes_options={'y': {'grid_lines': 'dashed'}})",
    "plt.show()"
]
